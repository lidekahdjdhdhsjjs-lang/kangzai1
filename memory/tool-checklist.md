# 🔧 工具使用最佳实践检查清单

> 最后更新: 2026-02-18 01:54

## exec 命令

**❌ 避免:**
- 直接使用 `pip install xxx`
- 使用未定义的命令
- 不检查命令退出码

**✅ 正确做法:**
```bash
# 使用 python3 -m pip 替代 pip
python3 -m pip install xxx

# 使用完整路径
/usr/bin/python3 -m pip xxx

# curl 替代方案
curl -s https://api.example.com/data
```

**常见退出码:**
- Code 1: 通用错误
- Code 35: SSL 连接错误
- Code 127: 命令未找到

---

## web_fetch

**❌ 避免:**
- 直接 fetch 需要认证的 URL
- fetch 外部不受信任的内容

**✅ 正确做法:**
```bash
# 使用 exec + curl 替代
curl -H "Authorization: Bearer $TOKEN" https://api.example.com

# 或使用公开 API
curl https://api.github.com/repos/owner/repo
```

---

## edit 工具

**❌ 避免:**
- 直接使用 old_string 而不重新读取文件
- 假设文件内容没变

**✅ 正确做法:**
1. **先读取文件**: `read` 工具获取最新内容
2. **精确复制**: 从读取结果中复制要修改的文本
3. **验证空白**: 确保不遗漏任何空格/换行
4. **使用 oldText 参数** (不是 old_string)

**参数名注意:**
- ✅ 使用 `oldText` 参数
- ❌ 不要使用 `old_string`

---

## message 工具 - react action

**❌ 避免:**
- 使用 react action 但不提供 target 参数

**✅ 正确做法:**
```json
{
  "action": "react",
  "emoji": "👍",
  "target": "message-id-here",
  "messageId": "message-id-here"
}
```

**必需的参数:**
- `emoji` - 必填
- `target` 或 `messageId` - 必填

---

## message 工具 - Unknown Channel

**❌ 避免:**
- 使用未配置的 channel 名称

**✅ 正确做法:**
- 检查 `channel` 参数是否正确
- 常用: `discord`, `telegram`, `whatsapp`
- 不带 channel 参数时使用默认

---

## read 工具 - ENOENT

**❌ 避免:**
- 假设文件存在
- 不处理文件不存在的情况

**✅ 正确做法:**
1. 先检查文件是否存在
2. 使用 `write` 创建缺失文件
3. 添加适当的错误处理

---

## 快速参考表

| 工具 | 高风险操作 | 防护措施 |
|------|-----------|---------|
| exec | pip, npm install | 使用 python3 -m pip |
| web_fetch | 认证 URL | 用 curl + header |
| edit | 精确匹配 | 先 read 再 edit |
| message:react | 缺 target | 始终加 target |
| read | 文件不存在 | 先 write 创建 |

---

*记忆：使用工具前先想清楚参数*
